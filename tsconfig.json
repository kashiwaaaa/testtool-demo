{
  "compilerOptions": {
    "target": "ESNext",              // もしくは "ES2017" 以上
    "module": "ESNext",
    "moduleResolution": "Node",
    "strict": true,
    "jsx": "preserve",
    "lib": ["ESNext", "DOM"],        // ← Promise を含む ES2015 以上を指定
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    }
  }
}
